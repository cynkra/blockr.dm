% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drill-filter-block.R
\name{new_drill_filter_block}
\alias{new_drill_filter_block}
\title{Drill filter block}
\usage{
new_drill_filter_block(state = list(columns = list()), ...)
}
\arguments{
\item{state}{List with \code{columns} — a list of column-object entries — and
\code{operator} ("&" or "|", default "&") controlling how the per-column
conditions combine on a data-frame input (same AND/OR pill as the
blockr.dplyr filter block; a builder option, so it sits in the gear
popover and shows once two or more fields are active). On a \code{dm} input
the operator is ignored: the restriction cascades per-table through
\code{dm::dm_filter()}, whose chaining is inherently AND, so the pill is not
offered there. Each column entry has \code{name} (column name), \code{table}
(source table; only when input is a \code{dm}), \code{mode} (\code{"single"} or
\code{"multi"}), and \code{values} (character vector of selected values). Logical
columns in single mode render as compact include-style checkbox rows:
checked requires \code{col \%in\% TRUE}, unchecked means no constraint from
that column (NOT a \code{FALSE} filter). To target FALSE/NA explicitly,
flip the column to Multi (values plus the \verb{<NA>} token) or use the
blockr.dplyr filter block. (Legacy saved states with a logical
single-select value of "FALSE" still evaluate as \code{!(col \%in\% TRUE)}.)
Old-style state (\code{list(columns=character, modes=list, values=list)})
is auto-migrated via \code{\link[=migrate_value_filter_state]{migrate_value_filter_state()}} for backward
compatibility.}

\item{...}{Additional arguments forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A block object.
}
\description{
The destination for cross-block drill claims: a \code{\link[=new_value_filter_block]{new_value_filter_block()}}
that senders can find without being told, and that resolves a claimed
column to a table in its own \code{dm}.
}
\details{
Put one on a board together with \code{blockr.viz::new_ctrl_bridge_extension()}.
The two go together: the bridge opens the control channel and this block is
what the channel writes into, so a board with a bridge and no drill filter
has senders with nowhere to send, and a drill filter without a bridge is
just a value filter.
}
\section{One per board}{

A sender resolves its target by class and takes it only when there is
exactly one candidate, so a second drill filter turns the drill back off
rather than picking a winner. Use plain value filter blocks for every other
filtering job on the board; they are not candidates and do not interfere.
}

\section{What it does with a claim}{

A claim is a list of \code{list(name=, mode=, values=)} entries pushed into the
block's \code{state} over the control channel. On a \code{dm} input each entry needs a
\code{table} as well, and this block fills it in:
\itemize{
\item exactly one table carries the column: that table.
\item several carry it (a subject id is in every ADaM table): the table where it
is the PRIMARY KEY, because \code{dm::dm_filter()} cascades along foreign keys,
so restricting the parent restricts the children too.
\item no table carries it: nothing is filtered and the block says which column
it could not place. This means the sender claimed a column it DERIVED, and
the fix belongs at the sender.
}

An entry that already names a valid table is left alone, so a sender that
does set \code{ctrl_table} keeps working unchanged.

On a plain data frame input there are no tables to resolve and this block
behaves exactly like a value filter.
}

\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.dm)
  serve(
    new_drill_filter_block(),
    data = list(data = datasets::iris)
  )
}

}
